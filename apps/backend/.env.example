# Copy this file to .env. Bun loads .env automatically for `bun run` scripts.
# When DATABASE_URL is unset, the backend falls back to the in-memory repository.
DATABASE_URL=mysql://rlyeh:rlyeh@localhost:3306/rlyeh
