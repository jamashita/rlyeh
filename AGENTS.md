# 文字の圧縮率・読解実験 仕様書

## 1. 実験の目的

言語・文字体系によって、同じ意味内容を表現するために必要な文字量が異なることに着目し、短時間の文章提示において、各言語の母語話者がどの程度の意味情報を取得できるかを比較する。

ここでいう「文字の圧縮率」は、同一または同等の意味内容を表現するときに必要となる文字数の少なさを指す。

例：

-   日本語：`国際化`
-   英語：`internationalization`

文字数はバイト数や Unicode
コードポイント数ではなく、その言語の母語話者が通常「1文字」と認識する単位を基本とする。

例：

-   日本語 `国際化` → 3文字
-   中国語 `国际化` → 3文字
-   韓国語 `한` → 1文字

ただし、本実験では単語単体ではなく、文章レベルで比較する。

------------------------------------------------------------------------

## 2. 基本的な実験方法

### 2.1 対象者

各言語について、その言語の母語話者を対象とする。

### 2.2 刺激

各言語で同一の意味内容を持つ文章を提示する。

翻訳は逐語訳ではなく、それぞれの言語として自然な文章にする。ただし、言語間で含まれる意味情報が変わらないようにする。

英語など特定の1言語を原文として他言語へ機械的に変換するのではなく、まず言語非依存の「意味内容」を定義し、それを各言語で自然に実現することを基本方針とする。

登場人物の名前は、各言語の母語話者にとって馴染みのある名前にする。馴染みのない外国語の名前は読むのに余計な時間がかかり、言語間で条件が揃わなくなるためである。名前は言語ごとに異なってよいが、性別など本文から読み取れる人物の属性は揃える。

### 2.3 提示時間

文章の提示時間は **2秒間** とする。

2秒間では全文を読み切れない程度の長さを使用する。

一方で、文章全体は1画面内に収め、スクロールやページ送りを必要としない。

### 2.4 表示条件

可能な限り以下を言語間で統一する。

-   表示領域
-   フォントサイズ
-   行間
-   文章の開始位置
-   その他、読解速度に不必要な影響を与える UI 条件

言語ごとの圧縮率の違いによって生じる行数の違いは、必ずしも人工的に揃えない。これは観測対象となる現象の一部である可能性があるためである。

------------------------------------------------------------------------

## 3. 文章作成方針

文章は以下の条件を満たすものとする。

### 3.1 文法的・意味的に自然であること

文法的に正しく、通常の意味理解が可能な文章を使用する。

完全なナンセンス文は使用しない。ナンセンス文では意味理解よりも単語列や短期記憶を測定する課題になりやすいためである。

### 3.2 内容の予測可能性を低くすること

文章全体には意味的な一貫性を持たせる一方、設問で問われる事実は、常識・世界知識・物語の典型的展開などから容易に推測できないものにする。

例えば、

> 巨大な機械が突然現れたとき、村人はどう感じたか。

のような設問は、「驚いた」「怖がった」などを本文を読まなくても推測できるため避ける。

一方、

> 最初に物体に触れたのは誰か。

のように、本文中の意味関係を取得しなければ答えにくい設問を優先する。

また、本文を作成する時点で、設問で問われる事実を物語の定番から外す。物語として自然な文章を書くと、設問で問われる事実も定番どおりになりやすく、本文を読まなくても推測で正答できてしまうためである。

例：

-   起動した装置が光る色 → SF の定番である青ではなく黄色にする。
-   物体に描かれた模様の数 → 神秘的なモチーフになりやすい3ではなく4にする。
-   第一発見者 → 物語の主人公になりやすい若い男性ではなく、別の人物にする。

ただし、物語全体が不自然になるほど定番を外す必要はない。本文の設定から自然に導かれる事実（例：嵐の翌朝に見つかった物体が、嵐によって露出したこと）は残してよい。

また、すべての事実を定番から外すと、「答えは常に意外なほう」という逆張りの推測で正答できてしまう。参加者は1回の参加で複数の本文を読むため、本文ごとに定番どおりの事実と定番から外した事実を混ぜ、本文を読まなければどちらか判断できないようにする。

### 3.3 不必要なワーキングメモリー負荷を避ける

設問は原則として **1問につき1つの命題・意味関係**を問う。

複数の人物・色・方向・順序などを同時に保持して結び付けなければならない課題を増やしすぎない。

例：

-   推奨：誰が物体に触れたか。
-   推奨：何がきっかけで機械が話し始めたか。
-   注意：複数人物の色・方向・行動の対応関係を同時に記憶させる。
-   注意：多数の出来事の詳細な順序を再構成させる。

2秒間提示後に回答する以上、短期記憶の影響を完全に排除することはできない。しかし、意味情報の取得とは無関係なワーキングメモリー負荷を意図的に増やさない。

### 3.4 設問間で情報を漏らさない

参加者は同じ本文について10問に続けて回答し、設問順はシャッフルされる。そのため、ある設問の文面や選択肢が他の設問のヒントになると、本文を読まなくても正答できてしまう。

-   ある設問の答えになる事実を、他の設問の設問文や選択肢に書かない。
-   設問文に、本文の事実を前提として含めない（例：「何年間、地中に埋まっていましたか」は「地中に埋まっていた」という別の設問の答えを漏らす）。
-   正答の選択肢だけが具体的・長いといった手掛かりを作らないよう、選択肢の粒度と長さを揃える。
-   設問を作成したら、本文を見せずに AI に解かせる。正答に付いた確率が偶然の正答率（1/6）を大きく上回る設問は作り直す。

### 3.5 選択肢に本文の語句をそのまま使う

選択肢では、本文の語句を言い換えずにそのまま使う（例：本文「赤い布」→ 選択肢「赤い布」）。

短時間の提示で単語が目に入り、その意味を処理できたこと自体が、本実験で測りたい意味情報の取得の一部であるためである。また、言い換えた選択肢は長くなりやすく、設問を読む負担が増える。

ただし、単語1つを見ただけで答えられる設問（何を使ったか、など）ばかりにならないよう、人物と行動の関係を問う設問（誰が触れたか、何がきっかけで話し始めたか、など）を多めに含める。

------------------------------------------------------------------------

## 4. 文章量

各言語で同じ単語数・文字数になるようには調整しない。

文字数を揃えてしまうと、測定対象である「同じ意味内容を表すのに必要な文字量の差」を消してしまうためである。

条件は以下とする。

-   意味内容を揃える。
-   各言語で自然な文章にする。
-   2秒では全文を読み切れない程度の情報量を持たせる。
-   全文を1画面に表示できる長さにする。

------------------------------------------------------------------------

## 5. 数字の表記

刺激本文・設問文・回答選択肢に含まれる数量には、原則として
**アラビア数字を使用しない**。

数字は、その言語で自然な数詞・単語として表記する。

例：

-   英語：`3 apples` → `three apples`
-   日本語：`3つの白い円` → `三つの白い円`
-   日本語：`25年間` → `二十五年間`

理由は、アラビア数字が言語横断的に非常に視認しやすく、短時間提示でも数量だけが瞬間的に取得される可能性があるためである。

各言語では、その言語の通常の文章として自然な数詞表記を採用する。

ただし、設問番号など、刺激内容そのものではない実験 UI
上の数字についてはこの制約の対象外とする。

------------------------------------------------------------------------

## 6. 設問形式

### 6.1 基本形式

各問題は以下の **6択** とする。

-   5つの具体的な回答候補
-   `本文からはわからない`

6つの選択肢からランダムに選択した場合の正答確率（偶然の正答率）は1/6となる。「本文からはわからない」も正答になり得るため、本仕様書では偶然の正答率を1/6として扱う。

### 6.2 「本文からはわからない」

すべての問題に、

> 本文からはわからない

という選択肢を設ける。

単なる「わからない」とはしない。

これは、

-   受験者本人が覚えていない
-   そもそも本文に答えが記載されていない

という2つを概念上区別するためである。

また、「本文からはわからない」は単なる棄権用の選択肢ではなく、**実際に正答となる問題も作成する**。

------------------------------------------------------------------------

## 7. 「本文からはわからない」が正解となる問題

本文には関連する情報が存在するが、設問が要求する具体的情報までは記載されていない問題を含める。

採用例：

本文：

> 機械は、自分が長年地中に埋まっていたと説明した。

設問：

> 機械は、何年間その場所にあったと説明しましたか？

選択肢：

-   十年間
-   二十五年間
-   五十年間
-   百年間
-   二百年間
-   本文からはわからない

正解：

> 本文からはわからない

本文には「長年」という情報は存在するが、具体的な年数は記載されていない。

この形式により、「本文からはわからない」を単なる逃げ道ではなく、他の選択肢と同等の正答候補として機能させる。

------------------------------------------------------------------------

## 8. 当てずっぽうへの対策

参加者が答えを取得できなかった場合でも、具体的な選択肢を推測で選ぶ可能性があることを前提とする。

そのため、「本文からはわからない」を自主的に選択してくれることだけには依存しない。

対策として以下を採用・検討する。

-   5つの具体的選択肢を用意する。
-   全問題に「本文からはわからない」を設ける。
-   「本文からはわからない」が実際の正解になる問題を混ぜる。
-   複数の問題を使用し、偶然の正答を平均化する。
-   選択肢の位置による手掛かりを避ける。
-   常識だけで正答できる設問を避ける。
-   公開前に、本文を見せずに AI に設問だけを解かせ、推測で正答できる設問を作り直す（§14.13）。

------------------------------------------------------------------------

## 9. 設問の順番

設問の順番は、**本文中で情報が登場する順番と一致させない**。

例えば、本文後半の情報についての問題を最初に出し、その後に本文前半の情報について質問するなど、本文位置と問題番号の対応を崩す。

これにより、

> 第1問だから本文の冒頭付近を思い出せばよい。

といった位置情報を利用した回答を防ぐ。

本番実装では、参加者ごとに設問順をランダムにシャッフルする。全言語で同じシャッフル方法を用いる。

選択肢も参加者ごとに A〜E の具体的回答候補の順番をシャッフルする。「本文からはわからない」は常に F に固定する。

実際に表示した設問順・選択肢順は回答とともに記録し、後から位置による影響を分析できるようにする。そのため、各設問にはどの命題を問うかを持たせ、命題の本文中の位置（§10.1）から「本文中のどの位置の情報を問うか」を求められるようにする。

------------------------------------------------------------------------

## 10. 刺激ファイル

刺激（本文・命題・設問・選択肢・正答）は `specs/stimuli/` 以下に1本につき1つの YAML ファイルとして管理する。ファイル冒頭のコメントに構成を記す。

| ファイル | 用途 |
| --- | --- |
| `specs/stimuli/practice.yaml` | 練習用。全参加者に固定で提示し、分析対象外とする |

### 10.1 命題の位置

各言語の本文では、各命題の内容を担う部分を `{p01:…}` の形で囲む。参加者に表示するときは囲みの記号を取り除く。

例（日本語）：

```text
{p03:倒れた木のそばで}、{p04:四つの白い円が描かれた黒い金属製の物体}を{p02:発見した}。
```

-   すべての命題を、各言語の本文でちょうど1回ずつ囲む。
-   囲みは入れ子にせず、重ねない。どの命題にも属さない部分（接続詞など）は囲まなくてよい。
-   1つの文に複数の命題が含まれ、語順が言語によって異なる場合があるため、命題の登場順は言語ごとに異なってよい。

命題の位置は、本文の先頭から囲みの開始位置・終了位置までの文字数（書記素単位、§14.8）として算出する。人が書くのは囲みだけで、文字数はプログラムで数える。

同じ命題でも言語によって本文中の位置（文字数）が異なる。2秒間で読み進められる量に限りがあるなら、同じ命題に少ない文字数でたどり着ける言語ほど正答率が高くなると予想される。この位置は §14.12 の分析で中心となる変数である。

刺激を追加するときは、囲みの形式（すべての命題がちょうど1回ずつ囲まれていること、囲みが入れ子や重なりになっていないこと）を自動で検査する。

------------------------------------------------------------------------

## 11. 練習用刺激の作成経緯

練習用刺激（`specs/stimuli/practice.yaml`）は、本文を見せずに AI（Gemini、ChatGPT）に解かせた結果に基づいて修正した。以後の本文・設問を作成する際の参考とする。

-   1回目：出来事の順序を問う設問の選択肢が、発見者・最初に触れた人物・円の変化を問う設問の答えを漏らしていることなどが判明した。順序を問う設問を削除し、§3.4 に基づいて修正した。
-   2回目：設問間の漏洩はほぼ解消したが、人物を問う選択肢で名前の有無が手掛かりになっていたこと、本文の事実が物語の定番どおりで推測しやすかったことが判明した。第一発見者と最初に触れた人物の入れ替え、円の数（3つ → 4つ）、円の変化後の色（青色 → 黄色）など、§3.2 と §3.4 に基づいて本文と設問を修正した。
-   3回目（Gemini）：10問すべてで正答に付いた確率が20%以下となった。
-   3回目（ChatGPT）：正答に付いた確率は平均で約22%。30%を超えたのは嵐による露出を問う設問（40%）のみで、これは本文の設定から自然に導かれる事実として意図的に残したものである。

2つのモデルの平均では、正答に付いた確率は偶然の正答率（1/6）と同程度となった。

英語版は上記の修正を反映した叩き台を作成済み。母語話者による確認、逆翻訳による照合、AI による事前テストは未実施。

------------------------------------------------------------------------

## 12. 現時点での検討事項

以下はまだ最終確定していない。

### 設問セットの複数化（将来の検討事項）

本文と設問が参加者によって公開される可能性がある（§14.14）。対策として、1本の本文に設問セットを複数用意し、参加者ごとにいずれかを提示する方法がある。作成と事前テストの手間が大きいため、現時点では採用しない。

### マスクの表示時間

本文の提示後に挟む白い画面は1秒未満（0.x 秒）とする。具体的な値は未確定。

------------------------------------------------------------------------

## 13. 設計上の中心原則

本実験で測定したいものは、単なる単語認識や暗記能力ではなく、

> **限られた提示時間内に、その言語で表現された文章からどれだけの意味情報を取得できたか**

である。

したがって、今後の文章・設問作成では以下を最優先する。

1.  言語間で意味内容を揃える。
2.  各言語として自然な文章にする。
3.  文字数・単語数そのものは揃えない。
4.  2秒間の固定提示とする。
5.  全文を1画面内に収める。
6.  常識だけで答えられる問題を避ける。
7.  選択肢は本文の語句をそのまま使い、単語1つで答えられる設問より意味関係を問う設問を多めにする。
8.  1問につき1つの意味関係を基本とする。
9.  不必要なワーキングメモリー負荷を避ける。
10. 「本文からはわからない」を実際の正答として含める。
11. 設問順を本文の情報提示順と一致させない。
12. 数量は各言語の自然な数詞表記を用い、刺激内ではアラビア数字を避ける。

------------------------------------------------------------------------

## 14. 運用方針

### 14.1 参加者

-   Web サイトとして公開し、知人経由で参加者を募集する。
-   母語は自己申告とする。参加者は必ず母語で受ける。
-   登録後に母語を変更することはできない。登録時に、選んだ母語で間違いないかを確認する画面を挟む。
-   母語が2つ以上ある参加者（バイリンガルなど）は、最も速く楽に読める言語を1つ選んで参加する。登録画面にその旨を案内する。
    -   複数の言語で参加することはできない。すべての言語で本文と設問の内容が同じため、2つ目の言語では内容を知った状態で読むことになるからである。
    -   登録時に、選んだ母語以外に母語として使う言語があるかを尋ね、あれば記録する。分析時に、バイリンガルの参加者を除外するか別に集計するかを選べるようにするためである。
-   母語の刺激が用意されていない場合は、言語の追加をリクエストしてもらう。
    -   リクエスト時には、メールアドレスと母語を受け付ける。この時点ではアカウントを作成しない。
    -   その言語が追加された時点で、リクエストしたメールアドレスでアカウントを自動で作成し、母語をリクエストした言語とする。作成したことをメールで知らせる。
    -   リクエスト時にも確認メールを送り、受信できるアドレスであることを確かめる。
    -   すでにアカウントがあるメールアドレス（正規化後の値で判定する。§14.7）からのリクエストは受け付けない。母語は変更できないためである。
    -   同意画面は、自動で作成したアカウントで初めて参加するときに表示する。
-   参加者は本番用の本文10本のうち、ランダムに選ばれた3本を受ける。各本文は1人1回のみ受けられる。
-   登録時、全参加者に参加者 ID を発行し、その参加者がどの本文を受けたか、どう回答したかを参加者 ID に紐付けて記録する。
-   登録時にメールアドレスの入力を必須とする。メールアドレスはアカウントとして機能する（パスワードは設けない）。別の端末やブラウザからでも同じ参加者として続きの本文を受けられる。
-   登録時に確認メールを送り、メールで届くリンクを開くことで登録を完了する。打ち間違えたアドレスや他人のアドレスでの登録を防ぐためである。
-   メールアドレスは、結果の送付、検証する言語・本文が増えたときの案内、言語追加のリクエストへの連絡に使う。
-   メールアドレスを入力せず、データの提供のみを行う参加形態は設けない。匿名で設問を下見してからメールアドレス付きで受け直す抜け道を塞ぐため、また結果の送付を希望するかどうかで参加者の取り組み方が変わり、分析が歪むことを避けるためである。
-   目標人数は各言語200人とする。比較対象とする言語がすべて目標人数に達するまでは、言語間比較の結果を見ない。
-   論文や学会などで正式に発表する予定はない。ただし、集計結果を知人など数名に見せることはある。
-   倫理審査は受けないが、同意画面とプライバシーポリシーを用意する。同意画面では、集計結果を第三者に見せる場合があること、見せるのは個人を特定できない集計値だけであることを明記する。

### 14.2 参加の流れ

1.  登録（同意画面、母語の申告、メールアドレスの入力）
2.  練習（固定の本文1本）。流れは本番と同じとする
3.  本番（10本の本文からランダムに3本）

本番用の本文は10本を用意する。本番の3本は、各言語の中で本文ごとの出題回数が偏らないように選ぶ。3本を受ける順番は参加者ごとにランダムとし、受験順を記録する。

後半に受けた本文ほど課題への慣れ（設問の傾向を知った上での読み方）の影響を受けるが、これは許容する。

本文ごとの流れは以下とする。

1.  参加者が開始ボタンを押すと、本文を2秒間提示する。カウントダウンや注視点は挟まない
2.  白い画面（マスク）を1秒未満（0.x 秒）挟む
3.  10問に回答する。各問は6択のラジオボタンで、制限時間は1問につき15秒とする

制限時間内に回答しなかった設問は未回答として記録し、次の設問へ進む。その設問に再び回答することはできない。未回答は、6つの選択肢のいずれとも異なる7番目の値（`unanswered`）として保持し、誤答とは区別する。未回答は画面上の選択肢としては表示しない。

### 14.3 離脱

-   離脱した参加者は、それ以上受けることができない。離脱した本文を再開することも受け直すことも、未着手の残りの本文を受けることもできない。そのため、本番の3本は1回の訪問で続けて受ける。
-   離脱時点で回答していない設問は未回答として扱う。
-   離脱前に回答を完了した本文は有効とする。
-   タブやアプリの切り替えは離脱とみなさない。切り替えている間も制限時間は減り続ける。
-   進捗と制限時間はサーバー側で管理する。リロードやブラウザの戻る操作で、提示や回答をやり直せないようにする。

### 14.4 表示条件

-   スマートフォンでの参加を認める。
-   最小の対象画面は iPhone SE（第3世代）の縦画面とする。論理解像度は 375×667 だが、Safari のツールバーを除いた実際の表示領域に収まることを基準とする。
-   フォントサイズは全言語で統一する。文字数が多くなりやすいフランス語を基準に、最小の対象画面で全文がスクロールなしに収まるようにする。
-   刺激を追加するときは、全言語の本文が最小の対象画面に収まるかを自動で検査する。
-   開始ボタンは本文の開始位置の近くに置く。本文が表示された瞬間に、視線が本文の書き出しから離れた位置にあることを避けるためである。

### 14.5 提示時間の精度

-   本文の表示開始と終了を実測し、実際に表示された時間を記録する。
-   実測値が2秒から大きく外れた試行は分析から除外する。除外の閾値は未確定。

### 14.6 不正検知

以下を検知・防止し、該当する試行に印を付ける。

-   同一ブラウザで2つ以上のタブを開くこと。BroadcastChannel で検知し、後から開いたタブをブロックする。
-   提示中・回答中のタブ切り替えやアプリ切り替え（`visibilitychange`, `blur`, `pagehide`）。試行は無効にせず、記録のみ行う
-   画面サイズの変化（Split View など）や画面の回転
-   本文の選択・コピー・長押しメニュー。これらは禁止する。
-   極端に速い回答。閾値は未確定。陸上競技のフライング判定（号砲から0.1秒未満の反応）を人間の反応の下限として参考にする

スクリーンショットや別端末での撮影は検知できない。

### 14.7 重複参加

参加者 ID 単位で受験済みの本文を管理し、同じ本文を2回受けられないようにする。

同じ受信箱に届くメールアドレスの別名（エイリアス）での再登録を防ぐため、メールアドレスを正規化した値で重複を判定する。送信には入力されたメールアドレスをそのまま使う。

-   全体を小文字にする。
-   ローカル部（`@` より前）の `+` 以降を取り除く（例：`taro+test@example.com` → `taro@example.com`）。
-   Gmail（`gmail.com`, `googlemail.com`）はローカル部の `.` を取り除き、ドメインを `gmail.com` に揃える。

以下の別名は正規化では検出できない。

-   iCloud の「メールを非公開」、Firefox Relay、DuckDuckGo Email Protection などが発行するランダムな転送用アドレス
-   独自ドメインのキャッチオール（任意のローカル部を受信する設定）
-   別のメールサービスで作った別のアドレス

使い捨てメールアドレスのサービス（登録直後に破棄できるもの）のドメインは、公開されているブロックリストを用いて登録を拒否する。

1人が別のメールアドレスで登録し直すことは完全には防げないため、以下を組み合わせて疑わしい参加に印を付け、分析時に除外する。IP を共有する正当な参加者がいるため、参加そのものは拒否しない。

-   登録時に localStorage / Cookie へ記録する
-   IP アドレス（ソルト付きハッシュ）と UA を記録する
-   参加前に過去の参加有無を自己申告してもらう

### 14.8 文字数の数え方

書記素（`Intl.Segmenter` の grapheme 単位）で数える。空白・句読点を含めるかは分析時に決める。

### 14.9 記録するデータ

-   回答（6つの選択肢のいずれか、または未回答 `unanswered`）、正誤、設問ごとの回答時間
-   本文の実際の提示時間
-   表示した本文・設問の識別子と版（刺激定義のハッシュ）
-   実際に表示した設問順・選択肢順、本文の受験順
-   UA、IP アドレス（ソルト付きハッシュで保存し、生の値は保存しない）
-   ビューポートサイズ、devicePixelRatio、フォントの読み込み状態
-   実際に表示された本文の行数と本文領域の高さ、各命題の囲みが表示された行（試行ごと。端末の画面幅によって折り返しが変わるため）
-   不正検知のイベント
-   自己申告の母語、およびそれ以外に母語として使う言語（ある場合）
-   メールアドレス（参加者 ID と対応させて回答データとは別に保存し、分析用に書き出すデータには含めない）

### 14.10 刺激の管理と意味内容の同一性

-   本文・設問・選択肢は YAML でリポジトリ内に管理する。
-   各本文について、言語に依存しない命題のリストを定義する。各設問はどの命題を問うかを持つ。
-   各言語の本文では、各命題の位置を囲みで示す（§10.1）。
-   各言語の本文は、下訳の後にその言語の母語話者が確認し、さらに逆翻訳で命題リストと照合する。

### 14.11 インフラ

-   アプリケーション: Cloud Run
-   回答データ: Cloud SQL

### 14.12 分析

データ収集を始める前に、仮説・目標人数・除外基準を含む分析計画を確定する。以下はそのたたき台である。

回答データは CSV などで書き出し、R（lme4）や Python（statsmodels）で分析する。

#### 分析の単位

-   1言語あたり、200人 × 3本 × 10問 = 最大6,000件の回答が得られる。
-   ただし、同じ参加者の回答どうし、同じ文章・設問への回答どうしは独立ではない。独立なサンプル数を6,000件とみなさず、参加者・文章・設問の入れ子構造を考慮して分析する。
-   設問の種類を、事実を問う設問、人物と行動の関係を問う設問、「本文からはわからない」が正答の設問に分けて扱う。

#### 未回答の扱い

-   未回答（`unanswered`）は、誤答とは区別して保持する（§14.2）。
-   正答率の主な分析では、未回答を「正答でない」として扱う。あわせて、未回答を除いた場合の結果も確認する。
-   未回答率は言語ごと・設問ごとに別途集計する。

#### 記述統計（言語ごと）

1.  全体の正答率と95%信頼区間。信頼区間は参加者単位のブートストラップで求める。
2.  参加者ごとの正答率の分布（ヒストグラム）。平均が同じでも、分布の形が違えば解釈が変わるため。
3.  文章ごとの正答率。文章による難易度の差を確認する。
4.  設問ごとの正答率。極端に簡単な設問・難しい設問を確認する。
5.  「本文からはわからない」の選択率を、それが正答の設問と誤答の設問に分けて集計する。前者だけが高ければ、参加者は本文に書かれていないことを識別できている。両方が高ければ、「覚えていなければ『本文からはわからない』を選ぶ」という答え方が使われている可能性がある。
6.  未回答率と回答時間の分布。

#### 主な分析

各回答の正誤（正答 = 1、それ以外 = 0）を目的変数とする混合効果ロジスティック回帰を用いる。

```text
logit(P(正答)) = β0
               + β1 × 命題の位置（本文の先頭からの文字数）
               + β2 × 受験順
               + 参加者の変量効果 + 文章の変量効果 + 設問の変量効果
```

-   命題の位置（§10.1）を中心の説明変数とする。同じ命題でも言語によって位置が異なるため、言語が少ない段階でも文字数と正答率の関係を段階的に調べられる。
-   命題の位置の代わりに、行数（その命題が表示された行）や本文全体の文字数・表示面積を説明変数とするモデルも比較する。文字数・行数・表示面積のどれが正答率をよく説明するか自体が、調べる価値のある結果である。
-   言語の違いを説明変数に加えたモデルも比較し、命題の位置で説明しきれない言語差が残るかを確認する。
-   「本文からはわからない」が正答の設問は、命題の位置との関係が他の設問と異なるため、主な分析から除き、上記5で別に扱う。

#### 解釈上の注意

-   言語が日本語と英語の2つだけの段階では、文字数の違いのほとんどが言語の違いに由来する。そのため、正答率の差が文字数によるものか、文字体系への慣れや参加者層の違いなど他の言語差によるものかを区別できない。この段階の結果は日英の比較として控えめに解釈する。
-   命題の位置を用いる分析は、参加者が本文を先頭から順に読むことを前提としている。2秒間では目立つ語を拾い読みする可能性もある。位置による差が見られない場合や、位置の効果の大きさが言語によって異なる場合は、それ自体を読み方の違いを示す結果として扱う。

#### 除外基準（案）

-   提示時間の実測値が2秒から大きく外れた試行（§14.5）
-   極端に速い回答（§14.6）
-   重複参加が疑われる参加者（§14.7）
-   バイリンガルの参加者は、除外した分析と含めた分析の両方を行う（§14.1）

閾値は分析計画の確定時に決める。

### 14.13 事前テスト（対照条件の代替）

本文を表示しない対照条件（提示時間ゼロ秒）を本番に設ければ、

-   ランダム回答による正答
-   設問文や選択肢だけからの推測
-   世界知識による推測

などによる基礎正答率を測定できる。

しかし、論文や学会などで正式に発表する予定はないため、対照条件は本番には組み込まない。代わりに公開前の事前テストとして、AI（Gemini、ChatGPT）に本文を見せずに設問だけを解かせ、その結果を判断基準とする。

手順：

1.  本文を見せずに、設問と選択肢だけを提示する。各設問について、選択肢 A〜F それぞれが正解である確率と、そう判断した理由を答えさせる。他の設問の文面や選択肢から推測できることは積極的に使わせる。
2.  Gemini と ChatGPT の両方で、それぞれ新しいチャットから実施する。

判断基準：

-   2つのモデルの平均で、正答に付いた確率が偶然の正答率（1/6）と同程度であること。
-   いずれかのモデルで正答に付いた確率が40%を超える設問は作り直す。ただし、§3.2 に基づいて定番どおりの事実として意図的に残した設問は除く。
-   理由に他の設問の文面や選択肢が挙がっている場合は、§3.4 の設問間の情報漏洩を疑い、該当する設問を修正する。

人間による事前テストは必須としない。実施する場合、事前テストで設問を見た本文については、その人は本番の測定に使えない。

### 14.14 結果の送付と刺激の漏洩

参加者が画面を録画するなどすれば、受けた本文と設問が公開される可能性がある。1人が受けるのは10本のうち3本のため、1人から公開されるのは一部にとどまるが、複数人から公開されればすべてが公開されうる。設問を事前に知った参加者は、答えの箇所だけを狙って読めるため、正答が漏れなくても測定が歪む。

これを完全に防ぐことはできないが、少なくとも運営側から正答を漏らさないよう、以下とする。

-   結果は、登録時のメールアドレスに送る。
-   送る結果は、同じ言語の参加者の中での偏差値とする。
-   参加者が少ないうちは偏差値が安定しないため、その言語の参加者が目標人数（200人）に達した時点で、バックグラウンドで統計処理を実行し、その言語の参加者全員に結果をメールで送る。参加者数は言語ごとに数える。
-   目標人数に達した後に参加した人には、参加の完了後にバックグラウンドで偏差値を算出し、結果をメールで送る。
-   設問ごとの正誤、正答、本文ごとの正答率は送らない。

同意画面に内容の口外禁止をあえて書くことはしない。書くことで、かえって内容の共有を意識させる恐れがあるためである。
